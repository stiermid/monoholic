name: "monoholic"
title: "monoholic"
email: hammond@example.com
description: >- # this means to ignore newlines until "baseurl:"
  Systems architect and low-level enthusiast. Focused on distributed systems, kernel optimization, and minimalist interface design.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username: "stiermid"
theme: "monoholic"


# Build settings
markdown: kramdown
theme: null # We are testing this theme locally, so we set theme to null or we use the local files

plugins:
  - jekyll-feed
  - jekyll-seo-tag

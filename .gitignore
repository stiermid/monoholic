*.gem
.bundle
.jekyll-cache
.sass-cache
_site

# Gemfile.lock is intentionally committed (theme doubles as GitHub Pages demo site)
# — ensures reproducible CI builds with bundler-cache

node_modules/
